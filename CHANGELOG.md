# Changelog

## 0.4.2-rc.2

- 请求隐私设置页正式跟随 DSH 全局语言，中文环境默认显示完整中文文案，运行时切换 English 后同步更新。
- 将中英文词典拆为独立、可测试模块，新增键集合与元数据标签完整性回归测试。
- 在真实 DSH 0.1.2-rc.1 WebUI 中完成中文启动、英文热切换、恢复中文以及零浏览器错误验收。

## 0.4.2-rc.1

- 增加上游 DeepSeek adapter 的来源、散列与补丁重放门禁，防止 vendored 运行时代码未经审计而漂移。
- npm 包明确限制到已验收的 Linux x86-64；发布物携带可复核的 provenance 与最小补丁。
- 将 DSH 提供的运行时模块声明为可选 peer，避免安装第二份宿主依赖树；新增真实 DSH 安装、重复安装、依赖、卸载门禁。
- 设置 RPC 拒绝多余字段、继承字段和非普通对象；补齐 HTTP 错误、截断 SSE、调用方取消和空闲超时测试。

## 0.4.1-rc.1 — npm 安装

- 提供预构建 npm 包，通过 DSH 原生 `plugin add` 安装，无需下载或解压 tgz。
- 中英文安装、升级说明改用明确版本的 npm 命令；保留 GitHub 安装包作为备用。
- 固定发布到 npm 官方源的 `next` 通道，不覆盖已有 GitHub Release。
- 隐私处理、默认设置及 DSH 0.1.2-rc.1 兼容范围不变。

## 0.4.0-rc.2 — 原生入口实时开关（候选版）

- 默认 DeepSeek 入口接入精简请求头，已有会话无需换 provider。
- 关闭时恢复原生请求头，不再阻止请求；正在生成的请求保持原模式。
- 设置开关自动保存，状态显示区分开启/关闭，不再混淆为配置来源。
- 默认不再过滤提示词或工具描述；明确文件上传、遥测、训练退出的边界。
- 新增原生 settings/LLM 到 HTTP 的四项集成测试，总计 16 项。
- 中英文 README 改为新手安装与使用指南；浏览器截图验收仍待完成。

## 0.4.0-rc.1 - 2026-09-04

- 固定适配 DSH 0.1.2-rc.1；迁移原生设置、客户端模块与 UI renderer 接口。
- 按官方安装后启动／重启流程发布，不增加宿主发现桥接，不承诺完整热安装。
- 重写中英文 README，补齐版本匹配、校验、凭据、路由选择、共享 prompt hook 范围、升级与卸载。
- 增加公开 CI、JUnit、包内容检查、独立消费者导入与校验和；历史 API 和 UI 证据标注版本及范围。

## 0.3.0 - 2026-08-18

- 支持 Cordis 配置禁用、显式 dispose 和 HMR 触发的热卸载/重新加载。
- 使用单一 `request-privacy-bundle` group 原子管理三个服务端入口；热关闭时先取消在途 DeepSeek HTTP/SSE 流与 WebUI 设置 RPC，再释放 provider、服务和监听器。
- WebUI 设置入口会随服务端拓扑在同一进程中动态撤下/恢复；静态 client bundle 的最终回收仍遵循 Harness 启动期 discovery 边界。
- 新增同进程 provider/Remote service 卸载、重载、活动连接关闭及真实 WebUI HMR 验收。

## 0.2.0 - 2026-08-18

- 将公开包名规范化为无 scope 的 `dsh-request-privacy`，符合 Harness `dsh-` 组合包前缀。
- 增加 GitHub 源码安装所需的 `prepare` 构建入口。
- 补齐中英双语加载、卸载、升级、验证说明和 WebUI 截图。

## 0.1.2 - 2026-08-18

- 精简 WebUI 文案，移除与请求设置无关的解释性说明。
- 使用红绿状态灯明确显示原生请求身份或覆盖后的请求身份。

## 0.1.1 - 2026-08-17

- 修复深色主题下白色“保存”按钮文字不可见的问题。
- 增加 Harness Docker 网关到独立捕获端的端到端出站身份验收。

## 0.1.0 - 2026-08-17

- 新增独立 `deepseek-private` provider 路由。
- 使用固定中性 `private-client` 身份并省略可选关联头。
- 新增 WebUI“请求隐私”设置选项卡。
- 新增 bounded settings RPC，不暴露凭据、端点或任意配置命名空间。
- 新增普通请求、压缩请求、禁用 fail-closed 和重定向拒绝测试。
- 新增打包产物组合启动与真实浏览器保存/刷新验收。
